<% (['development', 'staging', 'production'] + [@environment]).uniq.flatten.each do |env| -%>
<%= env %>:
  host: <%= @memcached[:host] || 'localhost' %>
  port: <%= @memcached[:port] || 11211 %>

<% end -%>
