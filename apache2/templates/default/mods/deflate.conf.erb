<% if node[:apache][:deflate_types] && !node[:apache][:deflate_types].empty? %>
<IfModule mod_deflate.c>
          AddOutputFilterByType DEFLATE <%= node[:apache][:deflate_types].join(' ') %>;
</IfModule>
<% end %>
