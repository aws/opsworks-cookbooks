package.CharlieCookbooks = {
    interfaces = (0.9.x);
    deploy = {
        generic = true;
    };
    build-system = no-op;
    build-tools = {
        0.9.x = {
            NoOpBuild = 1.0;
        };
    };
};
