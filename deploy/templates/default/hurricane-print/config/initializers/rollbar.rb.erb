Rollbar.configure do |config|
  config.access_token = '<%= @hurricane_print_settings[:rollbar_access_token] %>'

  config.exception_level_filters.merge!({
    'ActionController::RoutingError' => 'ignore'
  })  
end
