<%= @videl_env %>:
  senders:
    <%= @name %>:
      <% @conf.each do |type, options| %>
        <%= type %>:
          <% options.each do |key, value| %>
            <%= sprintf('%s: %s', key, value) %>
          <% end %>
      <% end %>
