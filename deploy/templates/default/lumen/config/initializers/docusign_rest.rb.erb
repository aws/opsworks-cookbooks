# This file was generated by the docusign_rest:generate_config rake task.
# You can run 'rake docusign_rest:generate_config' as many times as you need
# to replace the content of this file with a new config.

require 'docusign_rest'

DocusignRest.configure do |config|
  config.username       = '<%= @lumen_settings[:docusign][:username] %>'
  config.password       = '<%= @lumen_settings[:docusign][:password] %>'
  config.integrator_key = '<%= @lumen_settings[:docusign][:integrator_key] %>'
  config.account_id     = '<%= @lumen_settings[:docusign][:account_id] %>'
  config.endpoint     = '<%= @lumen_settings[:docusign][:endpoint] %>'
end

